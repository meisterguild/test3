# CLAUDE.md（テンプレートリポジトリ本体）

このファイルは、**`000-ai-template` リポジトリ自体を開発・保守する** Claude Code への指示書です。

> ※ プロジェクトへコピーされるテンプレート本体の指示書は `template/CLAUDE.md` です。
> このファイルの規約は `template/CLAUDE.md` を置き換えるものではなく、コピー先へは持ち込みません。

---

## 絶対規約: 応答言語

**すべての応答を必ず日本語で行うこと。**

- テキスト応答・エラー・確認メッセージはすべて日本語
- コード・コマンド・ファイルパス・URLは英語のまま
- 技術用語には初出時に日本語訳を併記する

---

## 絶対規約: 本リポジトリは二層構造

本リポジトリは、役割の異なる 2 つの層で構成される。

| 層 | 位置 | 役割 |
| ---------------- | ------------------- | ------------------------------------------------------------------ |
| ペイロード層 | `template/` 配下 | プロジェクトへコピーされるテンプレート本体 |
| メタ層 | `template/` の外 | テンプレートリポジトリ自身の説明・運用資産（コピー対象外） |

**コピー対象の判定基準はこの 1 行だけとする（契約点 C1）。ファイル単位の例外表を作らない。**

正準のコピーコマンドは次のとおり（契約点 C2）。末尾の `/.` はドットファイル（`.claude` / `.playwright` / `.gitignore`）を含めるために必須。

```bash
cp -r /path/to/000-ai-template/template/. /path/to/your-project/
```

### メタ層の構成

```text
000-ai-template/
├── README.md                         # テンプレートの使い方ガイド（メタ）
├── CLAUDE.md                         # このファイル（メタ）
├── .gitignore                        # このリポジトリ自身の git 管理設定（メタ）
├── .claude/
│   ├── settings.json                 # このリポジトリでの Claude Code 設定（push ゲートの配線）
│   └── hooks/push-review-gate.sh     # ペイロード側の実体へ委譲するフォワーダ
├── tests/                            # ペイロード側 hook の回帰テスト（メタ層）
└── template/                         # ← プロジェクトへコピーする本体（ペイロード）
```

### メタ層に置くテスト

ペイロードの hook（`template/.claude/hooks/**`）のロジックを固定する回帰テストは、
**メタ層の `tests/` に置く**（ペイロードには入れない）。理由は 2 つ。

- コピー先プロジェクトは自前の `tests/` を持つため、ペイロードに入れると衝突する
- hook のロジックを保守するのは本リポジトリの責務（規約 1 の判断基準に従う）

実行系は標準ライブラリの `unittest` に固定する（`python3 -m unittest discover -s tests`）。
`review-runner.py` 自体が「Python 3.8+ / 標準ライブラリのみ」で作られているため、
テスト側にも追加依存を持ち込まない。ハイフンを含むファイル名は通常の `import` が
できないため、`importlib.util.spec_from_file_location` でロードする。

---

## 作業時の規約

### 1. ファイルを追加・変更する前に、どちらの層に属するかを判断する

- **コピー先のプロジェクトで使うもの**（規約・ドキュメント・スキル・テンプレート・hook の実体）→ `template/` 配下に置く
- **このテンプレートリポジトリの説明・運用に属するもの**（使い方ガイド、本ファイル、リポジトリ自身の CI / git 設定）→ `template/` の外に置く

判断がつかない場合は推測で置かず、ユーザーに確認すること。

### 2. ペイロード内のパス記述は「コピー先プロジェクトのルート起点」で書く

`template/` 直下がコピー先のプロジェクトルートに対応する。したがって `template/CLAUDE.md`・`template/START_PROMPT.md`・各 `SKILL.md`・`template/docs/**` に書くパスは `docs/internal/...`・`.claude/skills/...` のように書き、**`template/` を前置きしてはいけない**（契約点 C3。前置きするとコピー先で壊れる）。

`template/` を含むパスを書いてよいのは、メタ層のファイル（`README.md` / 本ファイル）でリポジトリ内の位置を説明する場合だけ。

### 3. `templates/` はリネームしない

ドキュメントテンプレート置き場 `templates/`（現 `template/templates/`）は名称を変えない（契約点 C4）。単数 `template/`（ペイロード根）と複数 `templates/`（ドキュメントテンプレート）は別物として共存させる。

### 4. 構成を変えたら README.md を更新する

ペイロード／メタ層のファイル構成を変更したら、ルート `README.md` の「このリポジトリの構成」「ファイル構成」節を**必ず**同じ commit で更新すること。

### 5. push レビューゲートの解決順序を壊さない

`template/.claude/hooks/review-runner.py` と `template/.claude/hooks/push-review-gate.sh` は、基準ディレクトリを次の順で解決する（契約点 C5）。

1. `ROOT/.claude/review-config.yml` があれば `ROOT/.claude` を基準にする（＝コピー先プロジェクトでの通常動作）
2. 無ければ `ROOT/template/.claude` へフォールバックする（＝本リポジトリ内でのみ発火）

ゲートの実体はペイロード側のみに置き、メタ層 `.claude/hooks/push-review-gate.sh` は委譲専用のフォワーダに留める（二重管理をしない）。

ルート解決について**揃えるのは 1 段目（`CLAUDE_PROJECT_DIR` を最優先する）だけ**でよい。2 段目のフォールバック先は両者で意図的に異なる。

| | 1 段目 | 2 段目（フォールバック） |
| ------------------------ | ---------------------- | ------------------------------------ |
| フォワーダ（メタ層） | `CLAUDE_PROJECT_DIR` | 自身のスクリプト位置（`$0/../..`） |
| ゲート実体（ペイロード） | `CLAUDE_PROJECT_DIR` | `git rev-parse --show-toplevel` |

フォワーダが 2 段目にスクリプト位置を使うのは、フォワーダと実体が固定の相対関係にあるため。ここを消すと、`CLAUDE_PROJECT_DIR` が worktree やサブディレクトリを指す環境で必ず `exit 2` になり、**全 Bash コマンドがブロックされる**。

#### settings.json の同期義務

`.claude/settings.json`（メタ層）と `template/.claude/settings.json`（ペイロード）は、hook の配線が同一内容で重複している。**ペイロード側が真実源**であり、メタ層側はテンプレートリポジトリ自身で同じゲートを動かすための複製にすぎない。

- hook の配線を変更するときは **必ず両方を同じ内容に更新する**（片方だけ直すと、本リポジトリとコピー先で挙動が乖離する）
- 複製を許しているのは `settings.json` だけ。ゲートのロジック本体を両層に置くことは禁止（規約 5）

#### 既知の制約: ペイロードのみの変更は AI レビューが起動しない

`template/.claude/skills/code-security-review/references/security-gate.yml` のレビュー対象パターンは、契約点 C3 によりコピー先プロジェクトのルート起点（`.claude/hooks/**` 等）で書かれている。そのため本リポジトリ内で `template/` 配下だけを変更して push しても、パターンに一致せず AI レビューが起動しない。

ペイロード側の hook / スキル / ゲート判定ルールを変更したときは、**人手でのレビューを必ず挟むこと**。

> ⚠️ ルート `.claude/settings.json` の `PreToolUse` hook は**すべての Bash コマンド**で起動し、`$CLAUDE_PROJECT_DIR/.claude/hooks/push-review-gate.sh` が実行可能でなければ fail-closed で `exit 2` する。この配線を壊すと、実行中セッションの Bash が以後すべてブロックされる。`.claude/hooks/` を移動・削除する変更は、フォワーダの再設置（`chmod +x` まで）と**同一の Bash コマンド内**で完結させること。

---

## 禁止操作（ユーザーの明示的な指示がない限り実行禁止）

- `git push --force`（main / develop ブランチへの強制プッシュ）
- ペイロード内ドキュメントへの `template/` 前置き書き換え（規約 2 に反する）
- `template/templates/` のリネーム（規約 3 に反する）
- ルート `.claude/hooks/push-review-gate.sh` へのゲート本体の実装（規約 5 に反する）

---

## 不明点・リスクがある場合

- 推測で実装しない
- 確信が持てない場合は必ず質問してから進める
- 破壊的な変更をする前は必ず確認を取る
- 仕様にない内容を勝手に補完しない（不足・曖昧点は質問として抽出する）
