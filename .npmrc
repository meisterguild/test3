# package.json の engines を警告ではなくエラーにする。
# Node 20 などで install が通ってしまい、jsdom / vite の実行時に
# 原因の分かりにくい失敗になるのを防ぐ。
#
# 注意: この設定は依存パッケージが宣言する engines にも効く。将来 engines の上限を
# 切った transitive dependency が入ると install 自体が失敗する。その場合は
# 該当依存を更新するか、本行を外して README のバージョン記載での運用に戻す。
engine-strict=true
