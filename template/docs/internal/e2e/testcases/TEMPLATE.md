# <機能名> E2E テストケース

## 要件

- <テスト対象の機能仕様・改修内容を1〜3行で要約する>
- 参照: <要件定義書・基本設計書・GitHub Issue などへのリンク>

## 因子と水準

テストに関わる条件（因子）と、その取りうる値（水準）を洗い出す。条件の組み合わせが無い単純な機能では省略してよい。

| 因子 | 水準 |
| ---- | ---- |
| <例: 会員種別> | <例: ゲスト / 一般会員 / プレミアム会員> |
| <例: 支払い方法> | <例: クレジットカード / 代金引換> |

## デシジョンテーブル

因子の組み合わせパターンと、それを検証するケースの対応表。全組み合わせから間引いた場合は理由を備考に書く。「因子と水準」を省略した場合はこの表も省略してよい。

| # | <因子1> | <因子2> | 期待の要点 | ケースID | 備考 |
| - | ------- | ------- | ---------- | -------- | ---- |
| 1 | <水準> | <水準> | <この組み合わせで確認したいこと> | TC-<機能>-001 | |

## テストデータ

| 項目 | 値 |
| ---- | -- |
| 環境URL（ベースURL） | <http://localhost:xxxx など。アプリの起動方法が別途必要なら記載> |
| ユーザー情報 | <使用するテストユーザーの一覧。認証情報は環境変数名などの参照で書き、平文で書かない> |
| <その他のデータ> | <商品・マスタなど、ケースが依存するデータとその準備方法（シーダー等）> |

- 共通の前提条件: <全ケース共通の前提。無ければ「なし」>

---

## TC-<機能>-001: <ケースのタイトル>

- デバイス: PC / SP / 両方
- 優先度: 高 / 中 / 低

### 前提条件

- <このケース固有の前提（ログイン状態・データ状態など）>

### パラメータ

デシジョンテーブルの該当行（#）の因子値。組み合わせ検証でないケースでは入力値を直接書いてよい。

| 因子/項目 | 値 |
| --------- | -- |
| <因子名> | <このケースで使う水準・入力値> |

### 手順

1. <操作を1行ずつ書く。定型操作はフロー名で指定してよい（例: テストユーザーAでログインする（login-flow））>
2. <...>

### 期待結果

- <判定可能な事実を箇条書きで（例: カート画面に遷移する／「在庫がありません」エラーが表示される）>

### 実行結果

e2e-qa-tester が実行のたびに追記する（手で書かない）。判定の根拠・スクリーンショットは証跡リンク先を参照。

| 実行日時 | デバイス | 判定 | 証跡 |
| -------- | -------- | ---- | ---- |
| - | - | 未実行 | - |

---

## TC-<機能>-002: <次のケース>

（以下同じ構成で追加）
